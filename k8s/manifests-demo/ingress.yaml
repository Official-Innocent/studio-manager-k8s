apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: biggshots-demo-ingress
  namespace: biggshots-demo
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "500m"
spec:
  ingressClassName: nginx
  rules:
  - host: k3-demo.biggshots.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
